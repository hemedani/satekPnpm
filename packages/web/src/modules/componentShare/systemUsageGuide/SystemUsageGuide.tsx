import * as React from "react";
import { ContainerClient } from "../containerClient/ContainerClient";

export const SystemUsageGuide = () => {
  return (
    <ContainerClient
      colorHeader="blue"
      textHeader="نوشتن پیام جدید"
      withoutHeader={true}
      underConstruction={true}
    >
      {}
    </ContainerClient>
  );
};
